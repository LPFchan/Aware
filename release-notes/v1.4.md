## New in 1.4

- Pauses presence polling while the display is asleep, then resumes when the display wakes
- Pauses presence polling while the screen saver is active, then resumes afterward
- Cancels any in-flight camera capture immediately when Aware enters a paused state

## Notes

- This reissues `1.4` with a newer internal build so Sparkle can distinguish it from the earlier `1.4` build
- Sparkle release notes are now embedded directly from this release body instead of falling back to a bare GitHub changelog link

**Full Changelog**: https://github.com/LPFchan/Aware/compare/v1.3...v1.4